import React from 'react';
import { Icon } from '@ui-kitten/components';

export const CubeIconOutline = (style) => (
  <Icon {...style} name='email-outline'/>
);

export const FileTextIconOutline = (style) => (
  <Icon {...style} name='file-text-outline'/>
);
