import React from 'react';
import ContentView from '../../layouts/order/checkout';

export const CheckoutScreen = ({ navigation, route }) => (
  <ContentView navigation={navigation} route={route} />
);
