import React from 'react';
import ContentView from '../../layouts/history/index';

export const HistoryScreen = ({ navigation, route }) => (
  <ContentView navigation={navigation} route={route} />
);
